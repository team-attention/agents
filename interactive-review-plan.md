# Interactive Markdown Review Skill - Implementation Plan

## 1. 프로젝트 개요

### 문제점
1. Claude Code의 plan/review가 markdown으로 터미널에 나오면 길어질수록 가독성 저하
2. 개별 항목별로 피드백 주기가 번거로움 (전체를 텍스트로 다시 써야 함)

### 목표 플로우
```
Claude Code가 plan 생성
    ↓
웹 UI로 열림 (platform agnostic)
    ↓
각 항목에 checkbox ✅ 또는 comment 💬 남김
    ↓
저장/종료
    ↓
Claude Code가 피드백을 받아서 다음 동작 수행
```

### 기술 스택 결정
- **아키텍처**: Skill + MCP Server 조합
- **웹 UI**: Vanilla HTML/JS (의존성 없음, 단일 파일)
- **통신**: File-based (JSON)

---

## 2. 리서치 결과

### 유사 프로젝트: claude-review
- GitHub: https://github.com/Ch00k/claude-review
- Go 백엔드 + React 프론트엔드
- inline comment 방식
- 사용자가 URL을 직접 브라우저에서 열어야 함

### 우리 프로젝트와 차이점

| 항목 | claude-review | 우리 목표 |
|-----|---------------|---------|
| 브라우저 열기 | 수동 (URL 복사) | **자동** |
| 피드백 방식 | inline comment | **checkbox + comment** |
| Claude 전달 | 별도 명령어 | **submit 시 자동** |
| 기술 스택 | Go + React | **Python + Vanilla JS** |

### Claude Code 컴포넌트 비교

| 컴포넌트 | Blocking 가능? | 웹 UI 호스팅? | 상태 유지? |
|---------|---------------|--------------|-----------|
| **Skill** | ❌ | ❌ | ❌ |
| **Hook** | 제한적 (60초) | ❌ | ❌ |
| **MCP Server** | ✅ | ✅ | ✅ |

**핵심 발견**: Skill 단독으로는 "웹 열고 → 기다리고 → 결과 받기"가 **불가능**. MCP Server 필수!

---

## 3. 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                     Claude Code Session                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Claude가 plan 출력                                        │
│     ↓                                                        │
│  2. 사용자: "/review" 스킬 호출                                 │
│     ↓                                                        │
│  3. Skill → MCP Tool 호출 (start_review)                      │
│     ↓                                                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              MCP Server (Python)                      │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ start_review(markdown_content)                 │  │   │
│  │  │   1. 임시 HTML 파일 생성                         │  │   │
│  │  │   2. 로컬 HTTP 서버 시작                         │  │   │
│  │  │   3. webbrowser.open() 자동 실행               │  │   │
│  │  │   4. 결과 파일 polling (blocking)              │  │   │
│  │  │   5. 결과 JSON 반환                            │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│     ↓                                                        │
│  4. Claude가 결과 기반으로 동작                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Web Browser (localhost)                   │
├─────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Interactive Review UI                                 │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │ ## Implementation Plan                          │  │  │
│  │  │                                                 │  │  │
│  │  │ ☑️ Step 1: Create user model                    │  │  │
│  │  │    💬 [LGTM]                                    │  │  │
│  │  │                                                 │  │  │
│  │  │ ☐ Step 2: Add authentication                   │  │  │
│  │  │    💬 [JWT 대신 session 방식으로]                 │  │  │
│  │  │                                                 │  │  │
│  │  │ ☑️ Step 3: Write tests                          │  │  │
│  │  │    💬 []                                        │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │                                                        │  │
│  │  [Submit Review] ← 클릭 시 결과 저장 + 창 닫힘           │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    File-based Communication                  │
├─────────────────────────────────────────────────────────────┤
│  /tmp/claude-review-{uuid}/                                  │
│  ├── input.json      ← markdown content                      │
│  ├── result.json     ← 사용자 피드백 결과                       │
│  └── index.html      ← 생성된 UI                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 파일 구조

```
~/.claude/plugins/interactive-review/
├── .claude-plugin/
│   └── plugin.json           # 플러그인 매니페스트
├── skills/
│   └── review/
│       └── SKILL.md          # /review 스킬 정의
├── mcp-server/
│   ├── server.py             # MCP Server 메인
│   ├── web_ui.py             # HTML 생성기
│   └── requirements.txt      # 의존성
└── .mcp.json                 # MCP 서버 설정
```

---

## 5. MCP Server 구현 계획

### 핵심 로직

```python
# server.py 핵심 구조
class InteractiveReviewServer:

    @tool("start_review")
    async def start_review(self, content: str, title: str = "Review"):
        """
        1. UUID 기반 임시 디렉토리 생성
        2. Markdown → HTML 변환 (checkbox + comment 필드 추가)
        3. 로컬 HTTP 서버 시작 (port 자동 선택)
        4. webbrowser.open() 호출
        5. result.json 파일 polling (최대 5분)
        6. 결과 반환 또는 timeout
        """

    def _generate_html(self, markdown: str) -> str:
        """
        Markdown을 파싱해서:
        - 각 블록(heading, list item, paragraph)마다
        - checkbox + comment input 추가
        """

    async def _wait_for_result(self, result_path: str, timeout: int = 300):
        """
        파일 기반 polling으로 사용자 입력 대기
        """
```

### Tool Input/Output 스키마

```json
// Input
{
  "content": "## Plan\n- Step 1\n- Step 2",
  "title": "Implementation Review"
}

// Output
{
  "status": "completed",
  "items": [
    {
      "id": "block-1",
      "text": "## Plan",
      "checked": true,
      "comment": ""
    },
    {
      "id": "block-2",
      "text": "- Step 1",
      "checked": true,
      "comment": "LGTM"
    },
    {
      "id": "block-3",
      "text": "- Step 2",
      "checked": false,
      "comment": "이 부분은 다르게 해주세요"
    }
  ],
  "summary": {
    "total": 3,
    "approved": 2,
    "rejected": 1,
    "has_comments": 2
  }
}
```

---

## 6. Skill 정의 계획

### SKILL.md

```markdown
---
name: review
description: 마크다운 콘텐츠를 interactive하게 리뷰.
  "이거 리뷰해줘", "/review", "plan 검토", "피드백 받고 싶어" 등에 사용
allowed-tools: mcp__interactive_review__start_review, Read
---

# Interactive Review Skill

사용자가 마크다운 콘텐츠를 리뷰하고 피드백을 제공할 수 있도록 합니다.

## 사용 방법

1. 가장 최근 출력된 마크다운 콘텐츠를 수집
2. `mcp__interactive_review__start_review` 도구 호출
3. 웹 브라우저가 자동으로 열림
4. 사용자가 각 항목에 체크/언체크 + 코멘트 작성
5. Submit 후 결과를 받아서 처리

## 결과 처리

- checked=true, comment="" → 승인됨, 그대로 진행
- checked=true, comment="..." → 승인 + 추가 의견
- checked=false, comment="..." → 거부됨, 코멘트 내용대로 수정 필요
- checked=false, comment="" → 거부됨, 해당 항목 제거 또는 재검토 필요

## 예시 대화

사용자: "이 계획을 리뷰해줄 수 있어?"
→ Skill 자동 활성화 → 웹 UI 열림 → 피드백 수집 → 수정된 계획 제시
```

---

## 7. 웹 UI 설계

### 화면 구성

```
┌─────────────────────────────────────────────────────────────┐
│  📋 Interactive Review                              [Dark]  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ Block 1 ──────────────────────────────────────────────┐ │
│  │ ☑️  ## Implementation Plan                              │ │
│  │     ┌───────────────────────────────────────────────┐  │ │
│  │     │ Comment (optional)                            │  │ │
│  │     └───────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ Block 2 ──────────────────────────────────────────────┐ │
│  │ ☑️  ### Step 1: Create user model                       │ │
│  │     ┌───────────────────────────────────────────────┐  │ │
│  │     │ LGTM                                          │  │ │
│  │     └───────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ Block 3 ──────────────────────────────────────────────┐ │
│  │ ☐  ### Step 2: Add authentication                      │ │
│  │     ┌───────────────────────────────────────────────┐  │ │
│  │     │ JWT 대신 session 기반으로 해주세요               │  │ │
│  │     └───────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─ Block 4 ──────────────────────────────────────────────┐ │
│  │ ☑️  ### Step 3: Write tests                             │ │
│  │     ┌───────────────────────────────────────────────┐  │ │
│  │     │                                               │  │ │
│  │     └───────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ───────────────────────────────────────────────────────────│
│  Summary: 3/4 approved | 2 comments                         │
│                                                              │
│  [ ✅ Approve All ]  [ ❌ Reject All ]  [ 📤 Submit ]       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### HTML/JS 핵심 기능

```javascript
// 마크다운 블록 파싱
function parseMarkdown(content) {
  // heading, list item, paragraph, code block 별로 분리
  // 각 블록에 unique ID 부여
}

// Submit 핸들러
async function submitReview() {
  const items = collectItems();

  // 1. 결과를 result.json에 저장 (fetch POST)
  await fetch('/submit', {
    method: 'POST',
    body: JSON.stringify({ result: items })
  });

  // 2. 창 닫기
  window.close();
}

// 키보드 단축키
document.addEventListener('keydown', (e) => {
  if (e.metaKey && e.key === 'Enter') submitReview();
  if (e.key === 'Escape') window.close();
});
```

---

## 8. 파일 기반 통신 프로토콜

### 디렉토리 구조

```
/tmp/claude-review-{uuid}/
├── input.json      # MCP Server → Web UI
├── result.json     # Web UI → MCP Server (사용자 submit 후 생성)
└── index.html      # 생성된 웹 UI
```

### 통신 흐름

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│ MCP Server  │         │  HTTP Server │         │   Browser   │
└──────┬──────┘         └──────┬──────┘         └──────┬──────┘
       │                        │                       │
       │  1. Create temp dir    │                       │
       │  2. Write input.json   │                       │
       │  3. Generate HTML      │                       │
       ├───────────────────────►│                       │
       │  4. Start server       │                       │
       │                        │                       │
       │  5. webbrowser.open()  │                       │
       │                        │◄──────────────────────┤
       │                        │  6. GET /index.html   │
       │                        ├──────────────────────►│
       │                        │  7. Render UI         │
       │                        │                       │
       │                        │◄──────────────────────┤
       │                        │  8. POST /submit      │
       │                        │  {items: [...]}       │
       │                        │                       │
       │  9. Write result.json  │                       │
       │◄───────────────────────┤                       │
       │                        │                       │
       │  10. Polling detects   │  11. window.close()   │
       │      result.json       │──────────────────────►│
       │                        │                       │
       │  12. Return result     │                       │
       │                        │                       │
       ▼                        ▼                       ▼
```

### 데이터 스키마

```json
// input.json
{
  "version": "1.0",
  "timestamp": "2025-12-28T12:00:00Z",
  "title": "Implementation Review",
  "content": "## Plan\n- Step 1\n- Step 2",
  "blocks": [
    {"id": "block-0", "type": "heading", "level": 2, "text": "Plan"},
    {"id": "block-1", "type": "list-item", "text": "Step 1"},
    {"id": "block-2", "type": "list-item", "text": "Step 2"}
  ]
}

// result.json
{
  "version": "1.0",
  "timestamp": "2025-12-28T12:01:00Z",
  "status": "submitted",  // or "cancelled"
  "items": [
    {"id": "block-0", "checked": true, "comment": ""},
    {"id": "block-1", "checked": true, "comment": "LGTM"},
    {"id": "block-2", "checked": false, "comment": "다른 방식으로"}
  ]
}
```

---

## 9. 구현 단계 (Implementation Steps)

### Phase 1: MCP Server 기본 구조
1. Python MCP SDK 설치 (`mcp` 패키지)
2. `server.py` 기본 구조 작성
3. `start_review` tool 정의
4. 로컬 HTTP 서버 로직

### Phase 2: 웹 UI 구현
1. Markdown 파서 (블록 단위 분리)
2. HTML 템플릿 생성기
3. Checkbox + Comment UI
4. Submit/Cancel 핸들러

### Phase 3: 통신 레이어
1. 임시 디렉토리 관리
2. input.json / result.json 스키마
3. 파일 polling 로직
4. Cleanup 로직

### Phase 4: Plugin 패키징
1. plugin.json 작성
2. SKILL.md 작성
3. .mcp.json 설정
4. 설치 가이드

### Phase 5: 테스트 & 개선
1. 다양한 마크다운 형식 테스트
2. 타임아웃 처리
3. 에러 핸들링
4. UX 개선

---

## 10. 최종 요약

| 컴포넌트 | 기술 | 역할 |
|---------|-----|-----|
| **Skill** | SKILL.md | 트리거 + 컨텍스트 전달 |
| **MCP Server** | Python (mcp SDK) | 웹 서버 + 브라우저 열기 + blocking |
| **Web UI** | Vanilla HTML/JS | 사용자 인터랙션 |
| **통신** | File-based (JSON) | 결과 전달 |

---

## 참고 자료

- [Claude Code Plugins Docs](https://code.claude.com/docs/en/plugins)
- [claude-review](https://github.com/Ch00k/claude-review) - 유사 프로젝트
- [Claude Code Hook Development](https://github.com/anthropics/claude-code/blob/main/plugins/plugin-dev/skills/hook-development/SKILL.md)
- [Creating a Claude Code Plugin](https://dev.classmethod.jp/en/articles/claude-code-skills-subagent-plugin-guide/)
